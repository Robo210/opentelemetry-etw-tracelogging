[workspace]
members = ["bin", "lib"]

[workspace.dependencies]
opentelemetry = "0.18"
opentelemetry_api = {version="0.18", default-features = false, features=["trace"]}
opentelemetry-etw = { path = "lib" }
